;(function () {
  // Legacy placeholder to avoid missing-script errors when pages still reference js/ui/production.js.
  // Production pipeline logic now lives in prototype/ui/pipeline.js and is loaded where needed.
  // Keeping this file harmless ensures old references don't break local file:// loads.
})();
